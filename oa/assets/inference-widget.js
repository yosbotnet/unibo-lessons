(function(){'use strict';const host=document.getElementById('w-ci'),m=window.NotesInference;if(!host||!m)return;const nn=host.querySelector('#ci-n'),cl=host.querySelector('#ci-cl');function draw(){const r=m.calculate(Number(nn.value),Math.round(Number(cl.value)*10));host.querySelector('#ci-n-v').textContent=r.n;host.querySelector('#ci-cl-v').textContent=(r.level/10).toFixed(1)+'%';host.querySelector('#ci-out').textContent=m.summary(r);host.querySelector('[data-inference-width-plot]').innerHTML=m.svg(r);host.querySelector('[data-inference-width-table]').innerHTML=m.table(r);}for(const el of [nn,cl]){el.disabled=false;el.addEventListener('input',draw);}draw();})();
