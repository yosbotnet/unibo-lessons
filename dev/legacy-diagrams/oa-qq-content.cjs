'use strict';
const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict'),{execFileSync}=require('node:child_process');
const root=path.resolve(__dirname,'../..'),file='oa/cap-08-statistics.html';
const sources={plot:'https://www.itl.nist.gov/div898/handbook/eda/section3/normprpl.htm',ks:'https://www.itl.nist.gov/div898/handbook/eda/section3/eda35g.htm',qq:'https://www.statsmodels.org/stable/generated/statsmodels.graphics.gofplots.qqplot.html',shapiro:'https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.shapiro.html'};
const escape=s=>String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
const model=()=>JSON.parse(execFileSync('python3',[__dirname+'/oa-qq-model.py'],{encoding:'utf8'}));
function svg(m){assert.equal(m.n,7);const x=z=>70+(z+1.7)/3.4*590,y=v=>330-(v+6)/15*270;
 return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 700 410" style="width:700px" role="img" aria-labelledby="oa-qq-title" font-family="var(--lk-mono)" font-size="14" data-generated-plot="oa-normal-qq"><title id="oa-qq-title">Seven ordered observations against standard normal quantiles at midpoint plotting positions. The dashed line uses the observed mean and n-denominator scale; it is not a confidence boundary or a normality verdict.</title><rect width="700" height="410" fill="var(--lk-paper)"/>
<text x="16" y="24" fill="var(--lk-ink)">Ordered observation</text><text x="680" y="24" text-anchor="end" fill="var(--lk-ink)">n = 7 · midpoint positions</text>
${[-6,-3,0,3,6,9].map(v=>`<path d="M70 ${y(v)}H660" stroke="var(--lk-rule-soft)"/><text x="58" y="${y(v)+5}" text-anchor="end" fill="var(--lk-ink)">${v}</text>`).join('')}
${[-1.5,-1,-.5,0,.5,1,1.5].map(z=>`<path d="M${x(z)} 60V330" stroke="var(--lk-rule-soft)"/><text x="${x(z)}" y="355" text-anchor="middle" fill="var(--lk-ink)">${z}</text>`).join('')}
<path data-qq-line="" d="M${x(-1.7)} ${y(m.mean-1.7*m.scale)}L${x(1.7)} ${y(m.mean+1.7*m.scale)}" fill="none" stroke="var(--lk-cobalt)" stroke-width="1.5" stroke-dasharray="6 4"/>
${m.rows.map(r=>`<circle data-qq-point="${r.i}" cx="${x(r.z)}" cy="${y(r.observed)}" r="5" fill="var(--lk-vermilion)"><title>i = ${r.i}: z = ${r.z.toFixed(6)}, observation = ${r.observed}</title></circle>`).join('')}
<path d="M70 60V330H660" fill="none" stroke="var(--lk-ink)"/><text x="350" y="387" text-anchor="middle" fill="var(--lk-ink)">Standard normal quantile z = Φ⁻¹(p)</text></svg>`;
}
function table(m){return `<div class="figure-table" data-qq-table tabindex="0" role="region" aria-label="Q–Q coordinates: scroll horizontally"><table><caption>Full-precision calculations; displayed numbers rounded to six decimals</caption><thead><tr><th scope="col"><span class="qq-math">i</span></th><th scope="col">Observed</th><th scope="col"><span class="qq-math">pᵢ = (i − ½)/7</span></th><th scope="col"><span class="qq-math">zᵢ = Φ⁻¹(pᵢ)</span></th><th scope="col">Fitted quantile</th></tr></thead><tbody>${m.rows.map(r=>`<tr data-qq-row="${r.i}"><th scope="row">${r.i}</th><td>${r.observed}</td><td>${r.p.toFixed(6)}</td><td>${r.z.toFixed(6)}</td><td>${r.fitted.toFixed(6)}</td></tr>`).join('')}</tbody></table></div>`;}
function section(m=model()){return `<section id="s13" data-src="7 - Statistics_ inferential.pdf">
<h2>13. Assessing normality: plots, assumptions and tests</h2>
<p>Not every parametric method assumes a Gaussian distribution: a binomial model is parametric too. When a method does require normality, identify which quantity the assumption concerns—for example model errors or paired differences, not automatically every raw variable. Graphs and tests provide complementary diagnostics; neither certifies a model from a finite sample.</p>
<h3>1. Summary checks are not a calibrated normality test</h3>
<p>The slides propose comparing mean and median within 1% of the range and checking the 68–95–99.7 percentages with at least 50 observations. Treat those as informal teaching heuristics, not a universal threshold or required minimum. Symmetry alone does not imply normality (a uniform distribution is symmetric too). A random normal sample need not meet exact population percentages or have equal sample mean and median. Outliers can also inflate the range and make a range-based tolerance misleading.</p>
<h3>2. Histograms reveal shape, but depend on the bins</h3>
<p>Look for asymmetry, multiple modes and unusual tails, checking more than one sensible binning. Small samples make shape particularly uncertain. If a normal density curve is overlaid, use a density-normalized histogram or scale the curve to expected bin counts; a probability density and an unscaled count histogram have different vertical units. See the histogram widget in section 2 for how counts change with bin boundaries.</p>
<h3>3. A Q–Q plot pairs ordered observations with theoretical quantiles</h3>
<p>Sort the observations, assign increasing plotting probabilities pᵢ, then calculate zᵢ = Φ⁻¹(pᵢ), where Φ is the standard normal cumulative distribution function. Plot zᵢ horizontally and the ordered observations vertically. A roughly linear pattern is a useful diagnostic; a normal sample does not give an exact line. The <a href="${sources.plot}">NIST normal probability plot guide</a> uses a different plotting-position convention (approximate order-statistic medians). Here we retain the slides’ midpoint convention, pᵢ = (i − ½)/n. These are chosen probability positions, not a claim that actual random observations enclose exactly equal probability mass.</p>
<p>The seven original values are −4, −3, 0.8, 1.8, 3.9, 6.2 and 6.5. Their mean is ${m.mean.toFixed(6)}. Define dₙ = √[Σ(xᵢ − x̄)²/n] = ${m.scale.toFixed(6)}: this reproduces the slides’ rounded 3.85. The n−1 sample standard deviation is instead s = ${m.sampleSD.toFixed(6)}. This drawing deliberately uses dₙ, so the reference line is y = x̄ + dₙz; it is a descriptive fitted line, not an uncertainty interval.</p>
<figure class="lk-fig"><div class="figure-diagram" data-qq-plot tabindex="0" role="region" aria-label="Normal Q–Q plot: scroll horizontally">${svg(m)}</div><figcaption><b>Plate 8.8</b> — Vermilion points are the seven actual observations at their calculated standard-normal quantiles. The cobalt dashed line has intercept ${m.mean.toFixed(4)} and slope ${m.scale.toFixed(4)} in data units per z-unit. Its on-screen angle depends on axis scaling. Seven points alone cannot establish normality; there is no acceptance band in this drawing.</figcaption></figure>
${table(m)}
<p>The final column is qᵢ = x̄ + dₙzᵢ in the observations’ units. If qᵢ were used on the horizontal axis instead, the reference would be y = x, visually 45° only with equal unit scaling. Do not confuse these two axis conventions. Table and figure use unrounded pᵢ throughout; the displayed 0.071429 is not substituted back into the inverse CDF.</p>
<details data-qq-reproduce><summary>Reproduce the seven coordinates with Python’s standard library</summary><pre><code class="language-python">from statistics import mean, pstdev, NormalDist
x = sorted([-4, -3, 0.8, 1.8, 3.9, 6.2, 6.5])
mu, d = mean(x), pstdev(x)  # denominator n, matching this figure
for i, observed in enumerate(x, 1):
    p = (i - 0.5) / len(x)
    z = NormalDist().inv_cdf(p)
    print(i, observed, p, z, mu + d * z)</code></pre></details>
<p>Software defaults are part of the specification. In <a href="${sources.qq}">statsmodels.qqplot</a>, the default offset is a = 0, producing i/(n+1), and <code>line='q'</code> draws a quartile-based line. Those defaults do not reproduce this table or its mean/scale line. Use a = 0.5 for the midpoint positions and explicitly supply the intended reference line when recreating this figure.</p>
<h3>4. Statistical tests answer a specified null hypothesis</h3>
<p><a href="${sources.shapiro}">Shapiro–Wilk</a> tests the normal family with unspecified location and scale; it is not simply a KS test with fitted parameters. Anderson–Darling and D’Agostino’s K-squared are other normality procedures with their own calibration and requirements. A chi-square goodness-of-fit test requires choices of bins and appropriate expected counts. There is no universal power ranking: small samples can miss meaningful departures, while large samples can detect departures too small to matter for the intended analysis.</p>
<p>The usual one-sample <a href="${sources.ks}">Kolmogorov–Smirnov test</a> instead requires a fully specified continuous null distribution. Estimating its parameters on the tested sample invalidates the usual null calibration; a suitable fitted-parameter procedure such as the normal Lilliefors test, or a correctly calibrated simulation procedure, is needed. Independence and the sampling/model assumptions still matter.</p>
<pre data-normality-example><code class="language-python">${escape(fs.readFileSync(__dirname+'/oa-normality-example.py','utf8').trim())}</code></pre>
<p>This is synthetic data generated with mean 100 and standard deviation 20, not an observed data set known to be normal. A rejection of the standard N(0,1) null is not evidence against every normal distribution. The third test uses the generating parameters fixed independently of the sample. A p-value printed as zero can reflect numerical limits, not a literal probability of zero. Report the null, assumptions, statistic and p-value; say “do not reject” rather than “accept” or “proved Gaussian.”</p>
</section>`;}
const quizTitle='Describe the four ways to check normality that the deck gives, and the Q-Q plot procedure.';
const quiz='<p>Use summary heuristics, histograms, Q–Q plots and suitably calibrated tests as complementary diagnostics. Symmetry, the 1%-of-range heuristic and a sample-size cutoff do not establish normality. For this Q–Q example, sort the seven observations and use pᵢ = (i − ½)/7; plot Φ⁻¹(pᵢ) against the ordered observations. The line uses the mean and n-denominator scale, not a mandatory 45° angle. Other plotting-position and line conventions exist. Shapiro–Wilk tests the normal family; ordinary one-sample KS tests a fully specified continuous distribution. Non-rejection proves neither normality nor the assumptions of the intended analysis.</p>';
function next(html){let s=html;const re=/<section id="s13"[^>]*>[\s\S]*?<\/section>/;assert(re.test(s));s=s.replace(re,section());const marker='<summary>'+quizTitle+'</summary>',start=s.indexOf(marker);assert(start>=0);const end=s.indexOf('</details>',start);s=s.slice(0,start)+marker+'\n'+quiz+'\n      '+s.slice(end);if(!s.includes('href="assets/qq.css"'))s=s.replace('</head>','<link rel="stylesheet" href="assets/qq.css">\n</head>');return s;}
if(require.main===module){const old=fs.readFileSync(root+'/'+file,'utf8'),updated=next(old);if(process.argv.includes('--check')){assert.equal(updated,old);console.log('OA Q–Q plot, table and normality content synchronized');}else process.stdout.write('*** Begin Patch\n'+(updated!==old?'*** Update File: '+root+'/'+file+'\n'+require('./transfer-content.cjs').hunks(old,updated):'')+'*** End Patch\n');}
module.exports={file,model,svg,table,next,section,quizTitle,sources};
