'use strict';
const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');
const root=path.resolve(__dirname,'../..'),file='oa/cap-08-statistics.html',modelFile=__dirname+'/oa-dm-values.json';
const model=()=>JSON.parse(fs.readFileSync(modelFile,'utf8'));
const sources={code:'https://github.com/johntwk/Diebold-Mariano-Test/blob/c880f1af55a7552a020fdb4a166dc615b3fe74c2/dm_test.py',forecast:'https://pkg.robjhyndman.com/forecast/reference/dm.test.html',paper:'https://economics.sas.upenn.edu/sites/default/files/filevault/12-035.pdf',cd:'https://scikit-posthocs.readthedocs.io/en/latest/generated/scikit_posthocs.critical_difference_diagram.html'};
const escape=s=>s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
function svg(m){const x=i=>62+(i+.5)*49.5,y=v=>310-v/8000*232;assert.equal(m.n,12);assert(m.listResult.differences.concat(m.codeResult.differences).every(v=>v>=0&&v<=8000));return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 700 380" style="width:700px" role="img" aria-labelledby="oa-dm-title" font-family="var(--lk-mono)" font-size="14" data-generated-plot="oa-dm-alignment"><title id="oa-dm-title">Loss-difference audit of two conflicting MLP orderings in the slides. At each printed position, squared MLP error minus squared LSTM error. Blue uses the slide list; red uses the rotated code list. Neither ordering is certified as the correct temporal alignment.</title><rect width="700" height="380" fill="var(--lk-paper)"/><text x="16" y="24" fill="var(--lk-ink)">Squared-loss difference: MLP − LSTM</text>
<rect x="62" y="42" width="14" height="14" fill="var(--lk-cobalt)"/><text x="86" y="54" fill="var(--lk-ink)">Slide 66 list</text><rect x="352" y="42" width="14" height="14" fill="var(--lk-vermilion)"/><text x="376" y="54" fill="var(--lk-ink)">Slide 67 code</text>
${[0,2000,4000,6000,8000].map(v=>`<path d="M62 ${y(v)}H656" stroke="var(--lk-rule-soft)"/><text x="52" y="${y(v)+5}" text-anchor="end" fill="var(--lk-ink)">${v}</text>`).join('')}
${['list','code'].map((key,k)=>m[key+'Result'].differences.map((v,i)=>`<rect data-dm-bar="${key}-${i+1}" x="${x(i)+(k?2:-18)}" y="${y(v)}" width="16" height="${310-y(v)}" fill="var(--lk-${k?'vermilion':'cobalt'})" fill-opacity=".65"><title>Position ${i+1}, ${key} ordering: ${v.toFixed(6)}</title></rect>`).join('')).join('')}
${m.actual.map((_,i)=>`<text x="${x(i)}" y="335" text-anchor="middle" fill="var(--lk-ink)">${i+1}</text>`).join('')}<path d="M62 78V310H656" fill="none" stroke="var(--lk-ink)"/><text x="350" y="365" text-anchor="middle" fill="var(--lk-ink)">Printed position (not a verified forecast timestamp)</text></svg>`;}
function dataTable(m){return `<div class="figure-table" data-dm-table tabindex="0" role="region" aria-label="Both printed forecast orderings and their loss differences: scroll horizontally"><table><caption>All 12 printed positions; loss differences rounded to six decimals</caption><thead><tr><th scope="col">Pos.</th><th scope="col">Actual</th><th scope="col">MLP list</th><th scope="col">MLP code</th><th scope="col">LSTM</th><th scope="col">List loss gap</th><th scope="col">Code loss gap</th></tr></thead><tbody>${m.actual.map((a,i)=>`<tr data-dm-row="${i+1}"><th scope="row">${i+1}</th><td>${a}</td><td>${m.mlpList[i].toFixed(3)}</td><td>${m.mlpCode[i].toFixed(3)}</td><td>${m.lstm[i].toFixed(3)}</td><td>${m.listResult.differences[i].toFixed(6)}</td><td>${m.codeResult.differences[i].toFixed(6)}</td></tr>`).join('')}</tbody></table></div>`;}
function results(m){return `<div class="figure-table" data-dm-results tabindex="0" role="region" aria-label="Modified DM calculation under both printed orderings: scroll horizontally"><table><caption>Squared loss, h = 1, modified statistic and two-sided t reference with 11 df</caption><thead><tr><th scope="col">MLP order</th><th scope="col">Mean loss difference</th><th scope="col">Modified DM</th><th scope="col">p-value</th></tr></thead><tbody>${['list','code'].map(k=>{const r=m[k+'Result'];return `<tr data-dm-result="${k}"><th scope="row">Slide ${k==='list'?66:67} ${k}</th><td>${r.mean.toFixed(6)}</td><td>${r.statistic.toFixed(6)}</td><td>${r.pvalue.toFixed(8)}</td></tr>`;}).join('')}</tbody></table></div>`;}
function section(m=model()){return `<section id="s14" data-src="7 - Statistics_ inferential.pdf">
<h2>14. Student’s t, Diebold–Mariano and forecast alignment</h2>
<p>Student’s t is a family indexed by degrees of freedom. For an independent normal sample with unknown variance, the standardized sample mean uses a t reference with n−1 degrees of freedom. Unknown variance alone does not make every statistic exactly t-distributed. Other uses, including the modified forecast-comparison test here, have their own justification and limitations.</p>
<h3>Define the loss and the comparison before testing</h3>
<p>For aligned forecasts of the same target, let dₜ = L(e₁ₜ) − L(e₂ₜ). The Diebold–Mariano null is equal <em>expected loss</em>, E[dₜ] = 0, for the chosen loss and forecast setting—not equal predictions at every time or equal accuracy under every metric. Positive average d means the first forecast has larger observed loss. The <a href="${sources.paper}">author’s retrospective working paper</a> emphasizes comparing forecasts and making assumptions about their loss differences, rather than requiring knowledge of a model’s architecture.</p>
<p>The unmodified statistic is d̄ / √(Ω̂/n), where Ω̂ estimates the long-run variance of d. Its normal reference is asymptotic and needs suitable stationarity, moment and dependence conditions plus consistent variance estimation. Serial dependence, nonstationarity, parameter estimation and nested-model settings require care. “Model-free” does not mean assumption-free.</p>
<h3>The printed statistic uses a modified test, not a normal critical value</h3>
<p>The <a href="${sources.code}">implementation linked by the slides</a> applies the Harvey–Leybourne–Newbold adjustment and evaluates a two-sided t tail with n−1 degrees of freedom. The <a href="${sources.forecast}">forecast package</a> also documents a modified test. For this example n = 12 and h = 1: γ̂₀ = Σ(dₜ − d̄)²/n, the unmodified statistic is d̄/√(γ̂₀/n), and the adjustment multiplies it by √(11/12). This equals the ordinary t statistic of the loss differences algebraically; that identity does not prove those differences independent or normal.</p>
<p>Use the matching 5% two-sided critical values ±${m.codeResult.critical.toFixed(6)} from t₁₁, not ±1.96. With h = 1 this implementation keeps only lag zero; the horizon does not establish that all other autocovariances vanish. The modified t reference is not an exact finite-sample guarantee for arbitrary forecast errors, especially with only twelve observations.</p>
<h3>Audit the two conflicting MLP sequences</h3>
<p>The slide 66 list begins 414.231, 417.662, 381.815; the slide 67 executable example moves 414.231 to the end. Actual and LSTM arrays are unchanged. Without timestamped prediction exports and training/evaluation provenance, these sources do not identify which alignment is correct. Do not rotate predictions to obtain a more favorable score. Both calculations below preserve their respective printed orderings.</p>
<figure class="lk-fig"><div class="figure-diagram" data-dm-plot tabindex="0" role="region" aria-label="Forecast-alignment loss comparison: scroll horizontally">${svg(m)}</div><figcaption>Alignment audit — bars are calculated squared-error differences at the twelve printed positions. Cobalt uses the slide 66 MLP list; vermilion uses the rotated slide 67 code list. Tiny bars can be difficult to see on this common linear scale; exact source values and displayed loss differences are in the table. This is an audit of the supplied numbers, not newly trained models or verified out-of-sample performance.</figcaption></figure>
${dataTable(m)}
${results(m)}
<p>The code ordering reproduces modified DM = ${m.codeResult.statistic.toFixed(6)} and p = ${m.codeResult.pvalue.toFixed(8)}. The list ordering instead gives ${m.listResult.statistic.toFixed(6)} and ${m.listResult.pvalue.toFixed(8)}. Both arithmetic decisions reject at 5% under this specified procedure, but neither establishes that the data preparation or forecast alignment is valid. The p-value is not the probability that the difference was “due to chance,” and rejection is not proof of universal LSTM superiority.</p>
<p>For comparison, the <em>unmodified</em> code-order statistic is ${m.codeResult.raw.toFixed(6)}; a normal-tail calculation for that statistic gives ${m.codeResult.normalP.toFixed(8)}. Do not combine a statistic from one variant with a p-value or critical value from another. The LSTM values differ from all twelve actual values by at most 0.001; their mean squared error is ${m.codeResult.mse2.toExponential(6)}. This calls for checking the prediction pipeline and data availability, not assuming either extraordinary generalization or leakage from these numbers alone.</p>
<details data-dm-reproduce><summary>Reproduce both calculations (Python with SciPy)</summary><pre><code class="language-python">${escape(fs.readFileSync(__dirname+'/oa-dm-model.py','utf8').trim())}</code></pre></details>
<h3>Critical-difference diagrams summarize a defined analysis</h3>
<p>A <a href="${sources.cd}">critical-difference diagram</a> can show average ranks alongside pairwise post-hoc results. A crossbar denotes non-rejection under the chosen procedure and threshold, not equivalence or identical performance. Non-rejection need not be transitive, and ranks alone do not determine adjusted p-values. Distinguish a common critical-distance procedure from pairwise Wilcoxon tests with Holm adjustment; do not manufacture one shared distance for arbitrary pairwise results.</p>
<p>The slides describe five labelled methods, but the available extraction does not provide the full score matrix or adjusted pairwise p-values. Its specific crossbars cannot be numerically reproduced from this material alone. For multiple forecasting methods, choose valid comparisons and multiplicity handling for the actual dependence structure; adding models does not automatically justify a repeated-measures test on overlapping fold scores.</p>
<div class="callout exam" data-dm-limit><span class="callout-label">What makes a forecast comparison reproducible</span><p>Report target timestamps, forecast origins, horizon, training cutoffs and preprocessing; paired predictions and actuals; loss and direction; variance estimator and lags; correction and reference distribution; sample size, statistic, critical value and p-value; selection/multiplicity handling and relevant effect sizes. The unresolved source alignment is part of this example’s result, not a detail hidden by a significant p-value.</p></div>
</section>`;}
const quizTitle='State the Diebold-Mariano null hypothesis and decision rule, and read the deck\'s example.';
const quiz='<p>For aligned forecasts and a specified loss, DM tests E[dₜ] = 0. The slide-linked implementation uses an HLN-modified statistic with a two-sided t reference, not an unconditional normal law. At n = 12 and h = 1, the 5% critical value is about 2.200985. The code ordering gives 2.933300 and p = 0.01360984; the differently ordered slide list gives 3.429053 and p = 0.00563247. Both arithmetic results reject under that procedure, but the source alignment and out-of-sample provenance are unresolved. A p-value is not the probability of “chance,” and non-rejection in a critical-difference diagram is not equivalence.</p>';
function next(html){const re=/<section id="s14"[^>]*>[\s\S]*?<\/section>/;assert(re.test(html));let s=html.replace(re,section());const marker='<summary>'+quizTitle+'</summary>',start=s.indexOf(marker);assert(start>=0);const end=s.indexOf('</details>',start);s=s.slice(0,start)+marker+'\n'+quiz+'\n      '+s.slice(end);s=s.replace(/(<a href="#s14">)[^<]+(<\/a>)/,'$1Student’s t, Diebold–Mariano and forecast alignment$2');if(!s.includes('href="assets/dm.css"'))s=s.replace('</head>','<link rel="stylesheet" href="assets/dm.css">\n</head>');return s;}
if(require.main===module){const old=fs.readFileSync(root+'/'+file,'utf8'),updated=next(old);if(process.argv.includes('--check')){assert.equal(updated,old);console.log('OA forecast-alignment model, plot and inference content synchronized');}else process.stdout.write('*** Begin Patch\n'+(updated!==old?'*** Update File: '+root+'/'+file+'\n'+require('./transfer-content.cjs').hunks(old,updated):'')+'*** End Patch\n');}
module.exports={file,modelFile,model,svg,next,section,quizTitle,sources};
