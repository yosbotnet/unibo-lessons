# Deploy notes.ybc.sh — 8 settembre 2026

## Risultato

Pubblicato il lavoro completato, dopo la richiesta esplicita dell'utente:
«e poi fai il deploy del lavoro gia fatto».

- Produzione precedente: `f0f4bdeaca724112533411d98cc1a55681a777ea`.
- Rilascio contenuti e report: `738deb1`, che include il checkpoint `5659400`.
- Metodo: fast-forward del branch `main` nel repository già servito da Caddy,
  `/home/ybc/hosted/unibo-lessons`.
- 36 pagine di contenuto aggiornate rispetto alla produzione precedente, oltre
  agli asset, strumenti e documentazione della revisione.
- Questo verbale e la nota nel report di stato sono una successiva modifica
  solo documentale, pubblicata con lo stesso procedimento.
- Nessun push remoto, modifica della configurazione Caddy o riavvio di servizi.
- Le correzioni OA Parameter Fitting ancora soltanto diagnosticate non sono
  incluse: non esisteva codice nuovo di quel blocco da pubblicare.

## Verifiche effettuate per questo rilascio

### Prima della pubblicazione

- Nessuna modifica locale ai file tracciati della produzione e nessuna collisione
  fra i 75 file non tracciati preesistenti e i percorsi del rilascio.
- `git diff --check` sull'intero intervallo di rilascio.
- Test DL: 15 diagrammi, 122 collegamenti ancorati a porte, invarianti semantiche.
- Sincronizzazione: 15 diagrammi DL, cinque esempi cross-course, spazio di ricerca
  DM, 44 bundle condivisi e nove trasformazioni del capitolo di statistica OA.
- Test della separazione delle cache DL, Macro e Politics.
- Suite numerica OA inferenza: 39.200 combinazioni del widget, CDF e geometrie
  calcolate, campioni riproducibili e tre esempi Python eseguiti.
- 72 visite browser sulle 36 pagine cambiate, a 1280 e 390 px; 34 visite con prova
  di uno slider visibile, più verifica numerica esplicita del widget CI OA.
  Nessun errore JavaScript, overflow di pagina, elemento grafico fuori namespace,
  coordinata NaN/Infinity o immagine caricata ma non valida rilevati.
  Le dipendenze CDN di questo preflight usavano fixture locali.
- Controllo visivo di screenshot desktop/mobile selezionati per neurone DL e OA.

### Dopo la pubblicazione

- 502 risorse HTTP verificate, incluse tutte le 315 pagine HTML del sito:
  risposta 200 e corrispondenza esatta dei byte via HTTPS diretto a Caddy,
  con certificato validato.
- Le stesse 502 risorse rispondono 200 sul dominio pubblico: 187 coincidono
  direttamente; 305 HTML coincidono rimuovendo soltanto lo specifico script
  Cloudflare aggiunto alla risposta; altri 10 HTML coincidono anche invertendo
  l'offuscazione Cloudflare di 23 stringhe simili a indirizzi email e rimuovendo
  il relativo script di decodifica. Nessuna altra differenza residua.
- 22 visite browser sul dominio pubblico, su 11 pagine rappresentative a 1280
  e 390 px, con dipendenze esterne reali e senza fixture. Nessun errore JS,
  risposta HTTP >=400, overflow di pagina, grafica fuori namespace o immagine
  caricata ma non valida rilevati. Interazioni con widget in 14 visite.
- Widget OA: output confrontato con il modello per n=100 e livello 95%.
- PWA DL: cache `dl-v4-content-layout` attiva; pagina del neurone ricaricata
  offline con il diagramma generato presente.
- Tutti i 75 file non tracciati preesistenti risultano identici nei digest SHA-256.
  Le modifiche preesistenti a `review/index.html` e `review/report.md` nella
  worktree di preview sono rimaste fuori dal rilascio e non sono state alterate.

Gli esiti dettagliati sono in `/home/ybc/notes-legacy-review-artifacts/`:
`deploy-preflight-20260908.json`, `deploy-public-browser-20260908.json`,
`deploy-origin-http-20260908.json`, `deploy-public-http-normalized-20260908.json`
e `deploy-cloudflare-email-20260908.json`. I confronti iniziali non normalizzati
sono conservati: le loro differenze Cloudflare sono spiegate dai controlli successivi,
non cancellate o ignorate.

## Ripristino e limiti

Punto Git precedente: `backup/notes-predeploy-20260908`.
Archivio dei file tracciati precedenti:
`/home/ybc/notes-deploy-20260908-po1ErS/production-f0f4bde.tar.gz`.
SHA-256: `b9a6f006cef8030d441ec7c89ad2d13dc4d1d1ac6a822454f38b1a15a9ba8b29`.
L'archivio non comprende i file non tracciati, lasciati intatti; i loro digest
sono nello stesso percorso, in `untracked-before.json`.

Un eventuale rollback va preceduto da un nuovo controllo delle modifiche
concorrenti e limitato ai file del rilascio. Non usare reset distruttivi o
estrazioni indiscriminate sopra la produzione.

Questa verifica di deploy non certifica tutti i contenuti del sito e non
sostituisce i controlli scientifici ancora aperti nel
[report di avanzamento](stato-lavori-2026-09-08.md).
La trasformazione email di Cloudflare interessa anche esempi di codice e testo
SVG: lo stato senza JavaScript di quei dieci casi merita un controllo dedicato.
La configurazione Cloudflare non è stata modificata durante il deploy.
